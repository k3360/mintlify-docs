---
title: "获取剩余流量"
---

用户获取剩余流量
    **post** &nbsp; &nbsp;&nbsp; &nbsp;&nbsp; &nbsp;/account/getUsableTraffic

    **Body**
    ---
    <ParamField path="appKey" type="string" required>
        appKey密钥
    </ParamField>

    **Response**
    ---
    <ResponseField name="code" type="enum<integer>" defalut="default: 0">
        状态码，0为操作成功

        Available options: 0, -1
    </ResponseField>
    <ResponseField name="data" type="object">
        <Card>
            <ResponseField name="&nbsp;fullTraffic" type="integer">
                &nbsp;&nbsp;总流量
            </ResponseField>
            <ResponseField name="&nbsp;traffic" type="integer">
                &nbsp;&nbsp;剩余流量
            </ResponseField>
        </Card>
    </ResponseField>
    <ResponseField name="msg" type="string">
        响应消息
    </ResponseField>

        <RequestExample>
        ```bash cURL
            curl --request POST \
            --url https://apiv1.cakeip.com/openapi/account/getUsableTraffic \
            --header 'Content-Type: application/json' \
            --data '{
            "appKey": "<string>"
            }'
        ```
        ```bash Python
            import requests

            url = "https://apiv1.cakeip.com/openapi/account/getUsableTraffic"

            payload = {"appKey": "<string>"}
            headers = {"Content-Type": "application/json"}

            response = requests.request("POST", url, json=payload, headers=headers)

            print(response.text)
        ```
        ```bash JavaScript
            const options = {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: '{"appKey":"<string>"}'
            };

            fetch('https://apiv1.cakeip.com/openapi/account/getUsableTraffic', options)
            .then(response => response.json())
            .then(response => console.log(response))
            .catch(err => console.error(err));
        ```
        ```bash PHP
            <?php

            $curl = curl_init();

            curl_setopt_array($curl, [
            CURLOPT_URL => "https://apiv1.cakeip.com/openapi/account/getUsableTraffic",
            CURLOPT_RETURNTRANSFER => true,
            CURLOPT_ENCODING => "",
            CURLOPT_MAXREDIRS => 10,
            CURLOPT_TIMEOUT => 30,
            CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
            CURLOPT_CUSTOMREQUEST => "POST",
            CURLOPT_POSTFIELDS => "{\n  \"appKey\": \"<string>\"\n}",
            CURLOPT_HTTPHEADER => [
                "Content-Type: application/json"
            ],
            ]);

            $response = curl_exec($curl);
            $err = curl_error($curl);

            curl_close($curl);

            if ($err) {
            echo "cURL Error #:" . $err;
            } else {
            echo $response;
            }
        ```
        ```bash Go
            package main

            import (
                "fmt"
                "strings"
                "net/http"
                "io/ioutil"
            )

            func main() {

                url := "https://apiv1.cakeip.com/openapi/account/getUsableTraffic"

                payload := strings.NewReader("{\n  \"appKey\": \"<string>\"\n}")

                req, _ := http.NewRequest("POST", url, payload)

                req.Header.Add("Content-Type", "application/json")

                res, _ := http.DefaultClient.Do(req)

                defer res.Body.Close()
                body, _ := ioutil.ReadAll(res.Body)

                fmt.Println(res)
                fmt.Println(string(body))

            }
        ```
        ```bash Java
            HttpResponse<String> response = Unirest.post("https://apiv1.cakeip.com/openapi/account/getUsableTraffic")
            .header("Content-Type", "application/json")
            .body("{\n  \"appKey\": \"<string>\"\n}")
            .asString();
        ```
    </RequestExample>
    <ResponseExample>
        ```bash 200
            {
            "code": 0,
            "data": {
                "fullTraffic": 123,
                "traffic": 123
            },
            "msg": "<string>"
            }
        ```
        ```bash 400
            {
            "error": 123,
            "message": "<string>"
            }
        ```
    </ResponseExample>