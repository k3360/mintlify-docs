---
title: "获取白名单"
sidebarTitle: "获取白名单"
openapi: "POST /white/getWhitelist"
---

`POST` /white/getWhitelist

**请求体**

| 名称 | 类型 | 必选 | 说明 |
|---|---|---|---|
| appKey | string | 是 | 密钥 |

```JSON
{
    "appKey": "<string>"
}
```

**响应体**

| 名称 | 类型 | 说明 |
|---|---|---|
| code | int | 响应代码 |
| msg | string | 响应消息 |
| data | any  | 响应数据 |

<Tabs>
<Tab title="成功">
```JSON
{
  "code": 0,
  "msg": "<string>"
  "data": {},
}
```
</Tab>
<Tab title="失败">
```JSON
{
  "code": 1,
  "msg": "<string>"
}
```
</Tab>
</Tabs>

<RequestExample>
```bash cURL
curl --request POST \
--url https://open.ispkey.com/api/white/getWhitelist \
--header 'Content-Type: application/json' \
--data '{
"appKey": "<string>"
}'
```
```bash Python
import requests

url = "https://open.ispkey.com/api/white/getWhitelist"

payload = {
    "appKey": "<string>"
}
headers = {"Content-Type": "application/json"}

response = requests.request("POST", url, json=payload, headers=headers)

print(response.text)
```
```bash PHP
<?php

$curl = curl_init();

curl_setopt_array($curl, [
CURLOPT_URL => "https://open.ispkey.com/api/white/getWhitelist",
CURLOPT_RETURNTRANSFER => true,
CURLOPT_ENCODING => "",
CURLOPT_MAXREDIRS => 10,
CURLOPT_TIMEOUT => 30,
CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
CURLOPT_CUSTOMREQUEST => "POST",
CURLOPT_POSTFIELDS => "{\"appKey\":\"<string>\"}",
CURLOPT_HTTPHEADER => [
    "Content-Type: application/json"
],
]);

$response = curl_exec($curl);
$err = curl_error($curl);

curl_close($curl);

if ($err) {
    echo "cURL Error #:" . $err;
} else {
    echo $response;
}
```
```bash JavaScript
const options = {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({"appKey":"<string>"})
};

fetch('https://open.ispkey.com/api/white/getWhitelist', options)
.then(response => response.json())
.then(response => console.log(response))
.catch(err => console.error(err));
```
```bash Go
package main

import (
    "fmt"
    "strings"
    "net/http"
    "io/ioutil"
)

func main() {

    url := "https://open.ispkey.com/api/white/getWhitelist"

    payload := strings.NewReader("{\"appKey\":\"<string>\"}")

    req, _ := http.NewRequest("POST", url, payload)

    req.Header.Add("Content-Type", "application/json")

    res, _ := http.DefaultClient.Do(req)

    defer res.Body.Close()
    body, _ := ioutil.ReadAll(res.Body)

    fmt.Println(res)
    fmt.Println(string(body))

}
```
```bash Java
HttpResponse<String> response = Unirest.post("https://open.ispkey.com/api/white/getWhitelist")
.header("Content-Type", "application/json")
.body("{\"appKey\":\"<string>\"}")
.asString();
```
</RequestExample>
    </ResponseExample>
